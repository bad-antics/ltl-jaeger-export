// ltl-jaeger-export/main.ltl
//
// Jaeger Thrift over UDP span exporter.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-jaeger-export v0.1.0")
    io::println("Jaeger Thrift over UDP span exporter.")
    0
}

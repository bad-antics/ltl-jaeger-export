// examples/demo.ltl — runnable demo for ltl_jaeger_export

import std::io
use ltl_jaeger_export as lib

fn main() {
    io::println("ltl_jaeger_export demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}